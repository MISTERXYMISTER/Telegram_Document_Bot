server.port=${PORT:10000}

bot.username=docstore_ai_bot
bot.token=${BOT_TOKEN:YOUR_BOT_TOKEN}
bot.path=${BOT_PATH:/webhook}
external.url=${RENDER_EXTERNAL_URL:}

spring.datasource.url=${DATABASE_URL:postgres://user:pass@localhost:5432/db}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}